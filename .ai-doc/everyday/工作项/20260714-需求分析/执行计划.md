# 执行计划

## 已完成

1. 提取模型、字段、视图、字典、元数据和 Action 协议。
2. 实现可注入 Adapter 的服务端引擎及安全查询、写入和操作。
3. 实现 React Provider、HTTP Client、列表/详情/表单及扩展注册器。
4. 接入 Hono、Prisma、Neon、会话隔离和 OpenAPI。
5. 接入 Next.js 静态网站、漫画风格、双语界面和真实 Playground。
6. 补齐基础、自定义字段、自定义操作、Provider、API 和部署文档。

## 待完成

1. 运行全部测试、类型检查、静态构建和 Wrangler dry-run。
2. 浏览器验证 CRUD、语言切换、自定义字段和复制操作。
3. 通过 GitHub Actions 部署 API 与网站。
4. 验证 `mmd.zyking.xyz`、`mmd-api.zyking.xyz` 和在线 API 文档。
5. npm 发布前重新检查三个包名和打包内容。

## 回退

- 网站与 API 独立部署，可分别回退 Worker 版本。
- Neon 迁移保持向前兼容；部署失败时不执行破坏性迁移。
- 未配置或暂不可用 Neon 时，本地测试继续使用内存适配器。
